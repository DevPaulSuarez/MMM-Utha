# Migraciones

Cada cambio en la estructura de la base va aquí, en un archivo nuevo con número adelante (`001-…sql`, `002-…sql`). Se aplican con `php api/migrar.php`, en la computadora y después en el servidor. Ver el encabezado de `api/migrar.php`.
